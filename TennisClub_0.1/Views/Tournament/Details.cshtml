@model TennisClub_0._1.Models.TournamentViewModel

@{
    ViewData["Title"] = Model.Name;
    Layout = "_Layout";
}

<div>
    <h4>Toernooi: @Html.DisplayFor(model => model.Name)</h4>
    <hr/>

    <div class="container row mb-5">
        <img class="col-md-4" src="~/images/tournament.jpg" alt="">
        <div class="col-md-8">
            <div> @Html.DisplayFor(model => model.Name) </div>
            <div> @Html.DisplayFor(model => model.Description)</div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-2">
            <div class="mb-2">Prijs: @Html.DisplayFor(model => model.Price)</div>
            <div class="mb-2">Nog ? //TODO plekken over</div>
        </div>
        <div class="col-md-8"></div>
        <div class="col-md-2">
            <a class="btn btn-primary" asp-area="" asp-controller="Tournament" asp-action="Details" asp-route-id="1">Schrijf je hier in!</a>
        </div>
    </div>
</div>